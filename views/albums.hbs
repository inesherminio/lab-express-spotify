<h1>Artist's Album List</h1>
<div class="row">
{{#each dataAlbums}}
    <div class="card col-3" style="width: 18rem;">
        {{! There may be some albums without image assigned so, in order to avoid errors and bad UX/UI, you should use the the if/else helpers
        like you used in artists.hbs. If the image/url exists, then display it, if not/else display a generic image }}
        <img src="{{this.images.[0].url}}" class="card-img-top" alt="...">
        <div class="card-body">
            <h5 class="card-title">{{this.name}}</h5>
            <p class="card-text">Genres : {{this.genres}}</p>
            <a href="/album/tracks/{{this.id}}" class="btn btn-primary">View Tracks</a>
        </div>
    </div>   
{{/each}}
</div>