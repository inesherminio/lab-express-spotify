{{!-- You're not using this view (using home album-tracks instead), so should not have it in your project --}}

<div>
    <table>
        <tr>
            <th>Title</th>
            <th>Listen</th>
        </tr>
        {{#each tracks}}
        <tr>
            <th>{{this.name}}</th>
            <th>
                 <audio controls src="{{this.preview_url}}" >Your browser does not support the
            <code>audio</code> element.</audio>   
            </th>
        </tr>
        {{/each}}
    </table>