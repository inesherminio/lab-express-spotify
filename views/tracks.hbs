<div>
    {{#each tracks}}
{{!-- Inside each we need to call the data with this.name --}}
    <div>
        <p>{{this.name}}</p>

    </div>    
    <div>

        <audio controls src="{{this.preview_url}}" type="audio/mpeg">
             <code>audio</code>Listen in here!
        
        </audio>
    </div>
{{/each}}
</div>